export function DemoProjectStep() {
  return (
    <section className="rounded-lg border border-cyan-500/30 bg-cyan-500/10 p-4">
      <p className="text-sm uppercase tracking-wide text-cyan-300">Guided workflow</p>
      <h2 className="mt-1 text-xl font-semibold text-cyan-50">Generate a protected local project package</h2>
      <p className="mt-2 text-sm text-cyan-100">Supplier API keys are optional. Missing live data remains clearly marked instead of being replaced with fake stock.</p>
    </section>
  )
}
