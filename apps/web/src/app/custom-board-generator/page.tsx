import { GeneratorWorkspace } from '../../components/generator/GeneratorWorkspace'

// GeneratorWorkspace preserves the real outline editor and validation flow.
export default function CustomBoardGeneratorPage() {
  return <GeneratorWorkspace />
}
