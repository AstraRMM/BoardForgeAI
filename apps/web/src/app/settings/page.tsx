export default function SettingsPage() {
  return <main className="min-h-screen bg-slate-950 px-8 py-8 text-slate-100"><h1 className="text-3xl font-semibold">Settings</h1><p className="mt-3 text-slate-400">Configure the protected local engine, KiCad CLI, routing helper, and supplier API keys. Missing sourcing credentials stay visibly blocked; stock is never faked.</p></main>
}
