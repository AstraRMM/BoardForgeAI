export default function SettingsPage() {
  return <main className="min-h-screen bg-slate-950 px-8 py-8 text-slate-100"><h1 className="text-3xl font-semibold">Settings</h1><p className="mt-3 text-slate-400">Configure local engine path, KiCad CLI path, FreeRouting JAR, and sourcing API keys. Missing sourcing keys are reported as NOT_CHECKED, never faked.</p></main>
}
