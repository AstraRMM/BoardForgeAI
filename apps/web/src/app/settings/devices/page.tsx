export default function DeviceSettingsPage() {
  return (
    <main className="min-h-screen bg-slate-950 px-8 py-8 text-slate-100">
      <h1 className="text-3xl font-semibold">Devices</h1>
      <p className="mt-3 max-w-3xl text-slate-400">
        Device registration is scaffolded for local-first BoardForge installs. Project artifacts remain local drafts until explicitly approved for dashboard publishing.
      </p>
    </main>
  )
}
