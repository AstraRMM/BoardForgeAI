export default function UploadKicadPage() {
  return <main className="min-h-screen bg-slate-950 px-8 py-8 text-slate-100"><h1 className="text-3xl font-semibold">Upload KiCad Project</h1><p className="mt-3 max-w-2xl text-slate-400">BoardForge currently runs local-first. Use the CLI or KiCad plugin to select a local project folder, then this dashboard reads the generated manifests.</p></main>
}
