export function recommendNextActions({ health='Ready for Review', sourcing='SOURCABLE_WITH_WARNINGS', manufacturing='PCB_FAB_READY' }={}){ const actions=[]; if(sourcing.includes('WARNINGS'))actions.push('Review candidate alternatives and resolve unavailable or ambiguous BOM rows.'); if(manufacturing!=='PCB_FAB_READY')actions.push('Run Make Manufacturable before export.'); if(!actions.length)actions.push('Open reports, review limitations, and publish only with confirmation.'); return {status:'BOARD_FORGE_NEXT_ACTIONS_READY', health, sourcing, manufacturing, actions} }
