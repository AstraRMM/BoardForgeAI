{
  "id": "supplier_api_secret_handling_required_001",
  "title": "Supplier API credentials require explicit secret handling",
  "rootCause": "Supplier verification cannot become a product feature if secrets are hardcoded, logged, or committed.",
  "fixSummary": "Keep .env/.env.local ignored, commit only examples, report key presence and missing variable names without exposing values.",
  "appliesTo": ["sourcing", "security", "release_candidate"],
  "tests": ["npm run test:supplier-env-config", "npm run test:secret-handling-no-logs"]
}
