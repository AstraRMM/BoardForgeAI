import test from'node:test';import assert from'node:assert/strict';import manifest from'../../../fixtures/phase2c/50-board-challenge-manifest.mjs';import{catalogDefinition}from'../lib/phase2c/catalog-production-engine.mjs';import{logicAnalyzerProductionProposal as p,validateLogicAnalyzerArchitecture as validate}from'../lib/phase2c/templates/logic-analyzer.mjs'
test('Board041 requests multi-channel capture but catalog lacks an analyzer front end',()=>{const b=manifest.boards[40],d=catalogDefinition(b,40),g=validate(d);assert.equal(b.id,'041_LOGIC_ANALYZER');assert.equal(b.purpose,'multi-channel digital capture');assert.equal(d.topologyId,'rp2040-instrument');assert.equal(g.ok,false);for(const code of['logic-analyzer-capture-engine-missing','logic-analyzer-probe-connector-missing','logic-analyzer-input-protection-missing','logic-analyzer-input-buffer-missing','logic-analyzer-sample-clock-missing','logic-analyzer-trigger-missing','logic-analyzer-capture-memory-missing','logic-analyzer-host-interface-missing','logic-analyzer-channel-electrical-unverified','logic-analyzer-depth-streaming-unverified'])assert.ok(g.errors.includes(code),code)})
test('Board041 proposal preserves controller and translator capabilities without guessed limits',()=>{assert.match(p.status,/BLOCKED/);assert.deepEqual(p.candidates.map(x=>x.family),['Raspberry Pi RP2040','TI SN74AXC8T245']);assert.ok(p.candidates.every(x=>x.exactMpn===null));assert.equal(p.candidates[1].verified.notOvervoltageTolerantBeyondSelectedRailLimits,true);assert.ok(p.mandatoryUnresolved.some(x=>/channel count/i.test(x)));assert.ok(p.mandatoryUnresolved.some(x=>/simultaneous sample rate/i.test(x)));assert.ok(p.requiredTopology.some(x=>/guessed direction is not a universal analyzer front end/i.test(x)))})
test('logic analyzer gate accepts explicit electrical timing memory and calibration evidence',()=>{const roles=['capture controller','probe connector','input protection','input buffer level translator','sample clock','hardware trigger logic','capture memory buffer','capture USB host data interface'];const d={bom:roles.map((role,i)=>({ref:`X${i}`,role})),semanticEvidence:{logicAnalyzer:{channelElectricalVerified:true,inputLoadingProtectionVerified:true,sampleTimingVerified:true,triggerPerformanceVerified:true,captureDepthStreamingVerified:true,probeSignalIntegrityVerified:true,groundingIsolationPolicyVerified:true,hostThroughputVerified:true,productionCalibrationVerified:true}}};assert.deepEqual(validate(d),{ok:true,errors:[]})})
