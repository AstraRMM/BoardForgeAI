import test from'node:test';import assert from'node:assert/strict';import{stm32ControllerTemplate as t,validateStm32ControllerTemplate as validate}from'../lib/phase2c/templates/stm32-controller.mjs';import{approvedAssetFor}from'../lib/components/approved-production-assets.mjs'
test('template is coherent 5V CAN controller with no USB conflict',()=>{assert.deepEqual(validate(t),{ok:true,errors:[]});assert.equal(t.electrical.interfaces.some(x=>/USB/i.test(x)),false);assert.deepEqual(t.electrical.pinOwnership,{PA11:'CAN_RX',PA12:'CAN_TX'})})
test('MCU and CAN assets match physical package counts',()=>{for(const ref of['U1','U2']){const p=t.requirements.find(x=>x.ref===ref);assert.equal(approvedAssetFor(p.mpn,{requiredPinCount:p.pinCount}).mpn,p.mpn)}})
test('simultaneous USB and CAN pin ownership is rejected',()=>{const bad=structuredClone(t);bad.electrical.interfaces.push('USB2-device');assert.match(validate(bad).errors.join(','),/usb-can-pin-conflict/)})
test('MCP1700 keeps voltage margin below absolute maximum',()=>{assert.ok(t.electrical.inputVoltageV[1]<t.electrical.regulatorAbsoluteMaximumV);const bad=structuredClone(t);bad.electrical.inputVoltageV=[7,24];assert.match(validate(bad).errors.join(','),/regulator-input-margin-unsafe/)})
test('exact BOM includes termination TVS and decoupling',()=>{for(const role of['CAN_TERMINATION','CAN_TVS','MCU_DECOUPLING','CAN_DECOUPLING'])assert.ok(t.requirements.some(p=>p.role===role),role)})
