import test from'node:test';import assert from'node:assert/strict';import manifest from'../../../fixtures/phase2c/50-board-challenge-manifest.mjs';import{catalogDefinition}from'../lib/phase2c/catalog-production-engine.mjs';import{BOARD009_COMPACT_POE_AUDIT,BOARD009_EXACT_COURTYARD_AREA_PROOF,createBoard009IsolationWaistFixture,validateBoard009IsolationWaistFixture}from'../lib/phase2c/board009-isolation-waist-contract.mjs'
test('Board009 isolation waist is coherent and below 900mm2',()=>{const f=createBoard009IsolationWaistFixture(),r=validateBoard009IsolationWaistFixture(f);assert.equal(r.ok,true,r.errors.join('; '));assert.equal(r.areaMm2,804);assert.deepEqual(r.bounds,{minX:0,maxX:42,minY:0,maxY:20,width:42,height:20});assert.equal(f.holes.length,4);assert.equal(r.slotCount,2);assert.equal(r.corridorCount,4)})
test('Board009 fails closed when copper or mounting bridges the barrier',()=>{const f=createBoard009IsolationWaistFixture();f.isolationBarrier.allLayersKeepout=false;f.holes[0].x=21;f.corridors[0].maxX=20;f.slots.pop();const e=validateBoard009IsolationWaistFixture(f).errors;for(const x of['board009-primary-secondary-barrier-unverified','board009-mount-bridges-isolation','board009-routing-crosses-isolation-keepout','board009-isolation-slots-unverified'])assert.ok(e.includes(x),x)})
test('Board009 catalog projects the compact isolation fixture',()=>{const d=catalogDefinition(manifest.boards[8],8),f=createBoard009IsolationWaistFixture();assert.deepEqual(d.outlinePoints,f.outline);assert.deepEqual(d.holes,f.holes);assert.equal(d.widthMm,42);assert.equal(d.heightMm,20);assert.equal(d.placementTopologyId,'poe-sensor-isolation-waist')})
test('Board009 exact packages prove the 900mm2 envelope geometrically impossible',()=>{const p=BOARD009_EXACT_COURTYARD_AREA_PROOF;assert.equal(p.geometricallyPossible,false);assert.ok(p.minimumAreaMm2>p.maximumAreaMm2);assert.equal(Number(p.packages.reduce((sum,x)=>sum+x.areaMm2,0).toFixed(4)),p.minimumAreaMm2);assert.match(p.reason,/before clearance, holes, isolation, or support components/)})
test('Board009 compact audit rejects the best stocked authoritative PoE pair by a through-hole packing lower bound',()=>{const p=BOARD009_COMPACT_POE_AUDIT,best=p.magjackCandidates[0];assert.equal(p.geometricallyPossible,false);assert.equal(best.liveStock.digikey>0&&best.liveStock.mouser>0,true);assert.equal(best.throughHole,true);assert.equal(p.isolatedPd.throughHole,true);assert.equal(best.pairWidthMm,45.2);assert.ok(best.pairWidthMm>p.envelopeMm.width);assert.match(p.reason,/before the 6 mm isolation waist/)})
