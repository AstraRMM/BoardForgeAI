import test from'node:test';import assert from'node:assert/strict';import manifest from'../../../fixtures/phase2c/50-board-challenge-manifest.mjs';import{catalogDefinition}from'../lib/phase2c/catalog-production-engine.mjs';import{environmentalLoggerProductionProposal as p,validateEnvironmentalLoggerArchitecture as validate}from'../lib/phase2c/templates/environmental-logger.mjs'
test('Board023 requests long-duration logging but catalog maps only a PD supply',()=>{const b=manifest.boards[22],d=catalogDefinition(b,22),g=validate(d);assert.equal(b.id,'023_ENVIRONMENTAL_LOGGER');assert.equal(b.purpose,'long-duration environmental logging');assert.equal(d.topologyId,'usb-c-pd-sink');assert.equal(g.ok,false);for(const code of['logger-environmental-sensor-missing','logger-nonvolatile-storage-missing','logger-timebase-missing','logger-controller-missing','logger-power-management-missing','logger-data-retrieval-missing','logger-measurement-requirements-unverified','logger-storage-capacity-endurance-unverified','logger-runtime-unverified','logger-write-recovery-unverified'])assert.ok(g.errors.includes(code),code)})
test('Board023 proposal keeps sensor and RTC selections non-exact and capacity gated',()=>{assert.match(p.status,/BLOCKED/);assert.deepEqual(p.candidates.map(x=>x.family),['TI HDC302x','Bosch BMP390','Micro Crystal RV-3028-C7']);assert.ok(p.candidates.every(x=>x.exactMpn===null));assert.ok(p.mandatoryUnresolved.some(x=>/measurands/i.test(x)));assert.ok(p.mandatoryUnresolved.some(x=>/storage capacity\/endurance/i.test(x)));assert.ok(p.requiredTopology.some(x=>/atomic records/i.test(x)))})
test('environmental logger gate accepts explicit sensing storage runtime and recovery evidence',()=>{const roles=['environmental sensor','nonvolatile log storage','RTC timestamp timebase','data logger controller','battery management autonomous power','data export service connector','brownout supervisor watchdog'];const d={bom:roles.map((role,i)=>({ref:`X${i}`,role})),semanticEvidence:{environmentalLogger:{measurementRequirementsVerified:true,calibrationVerified:true,storageCapacityEnduranceVerified:true,timestampAccuracyVerified:true,powerRuntimeVerified:true,enclosureExposureVerified:true,interruptedWriteRecoveryVerified:true,productionTestVerified:true}}};assert.deepEqual(validate(d),{ok:true,errors:[]})})
