import test from'node:test';import assert from'node:assert/strict';import{createPhase2cChallengeDriver}from'../../../scripts/phase2c-challenge-driver.mjs'
const board={id:'006_INDUSTRIAL_IO',slug:'industrial-io',architectureClass:'industrial-control'}
test('Board006 invokes only its injected generator at manifest index five',async()=>{let seen;const driver=createPhase2cChallengeDriver({generateIndustrialIo:async input=>{seen=input;return{acceptance:{accepted:true},manufacturingEvidence:{status:'MANUFACTURING_ACCEPTED'}}}});const result=await driver.executeBoard(board,{index:5});assert.equal(result.acceptance.accepted,true);assert.equal(seen.template.id,'006_INDUSTRIAL_IO');assert.equal(seen.context.index,5)})
test('Board006 is fail closed at a wrong index',async()=>{let called=false;const driver=createPhase2cChallengeDriver({generateIndustrialIo:async()=>{called=true}});const result=await driver.executeBoard(board,{index:6});assert.equal(called,false);assert.equal(result.failure.code,'BOARD_CLASS_ENGINE_NOT_IMPLEMENTED')})
test('Board006 cannot counterfeit strict production acceptance',async()=>{const result=await createPhase2cChallengeDriver({generateIndustrialIo:async()=>({acceptance:{accepted:true},manufacturingEvidence:{status:'MANUFACTURING_REJECTED'}})}).executeBoard(board,{index:5});assert.equal(result.failure.code,'STRICT_MANUFACTURING_ACCEPTANCE_FAILED')})
