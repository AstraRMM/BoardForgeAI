import test from'node:test';import assert from'node:assert/strict';import manifest from'../../../fixtures/phase2c/50-board-challenge-manifest.mjs';import{catalogDefinition}from'../lib/phase2c/catalog-production-engine.mjs';import{powerMonitorProductionProposal as p,validatePowerMonitorArchitecture as validate}from'../lib/phase2c/templates/power-monitor.mjs'
test('Board045 requests multi-rail telemetry but catalog maps only a PD supply',()=>{const b=manifest.boards[44],d=catalogDefinition(b,44),g=validate(d);assert.equal(b.id,'045_POWER_MONITOR');assert.equal(b.purpose,'multi-rail power telemetry');assert.equal(d.topologyId,'usb-c-pd-sink');assert.equal(g.ok,false);for(const code of['power-monitor-rail-connectors-missing','power-monitor-shunts-missing','power-monitor-converter-missing','power-monitor-input-protection-missing','power-monitor-telemetry-controller-missing','power-monitor-isolation-ground-policy-missing','power-monitor-temperature-sensing-missing','power-monitor-host-interface-missing','power-monitor-rail-count-undeclared','power-monitor-shunt-fault-energy-unverified'])assert.ok(g.errors.includes(code),code)})
test('Board045 proposal preserves multi-channel and per-rail monitor alternatives',()=>{assert.match(p.status,/BLOCKED/);assert.deepEqual(p.candidates.map(x=>x.family),['TI INA3221','TI INA228']);assert.ok(p.candidates.every(x=>x.exactMpn===null));assert.ok(p.mandatoryUnresolved.some(x=>/rail count/i.test(x)));assert.ok(p.mandatoryUnresolved.some(x=>/shared versus floating grounds/i.test(x)));assert.ok(p.requiredTopology.some(x=>/must not be joined merely to use a multi-channel device/i.test(x)))})
test('power monitor gate accepts explicit rails shunts isolation and calibration evidence',()=>{const roles=['rail connector busbar terminal','Kelvin current shunt','power monitor','sense input shunt protection','power measurement telemetry controller','common-ground telemetry policy','shunt temperature sensor','host telemetry connector'];const d={bom:roles.map((role,i)=>({ref:`X${i}`,role})),semanticEvidence:{powerMonitor:{railCount:4,railRangesCommonModeVerified:true,shuntFaultEnergyVerified:true,monitorRangeTimingVerified:true,isolationGroundingVerified:true,uncertaintyBudgetVerified:true,thermalBusbarVerified:true,safetyProtectionVerified:true,calibrationTraceabilityVerified:true,productionRailTestVerified:true}}};assert.deepEqual(validate(d),{ok:true,errors:[]})})
