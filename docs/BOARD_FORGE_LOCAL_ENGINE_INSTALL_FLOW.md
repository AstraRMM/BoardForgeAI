# BoardForge Local Engine Install Flow

Install local dependencies, run `npm run boardforge:doctor`, start the installed local engine with `npm run boardforge:start`, pair the live website to the local engine, then run the public alpha demo. The live website does not receive supplier secrets.
